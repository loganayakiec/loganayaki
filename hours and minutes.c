#include<stdio.h>
#include<conio.h>
void main()
{
int hour,minute1,total;
printf("hour:");
if(scanf("%d",&hour)==1)
{
pirntf("minute:");
if(scanf("%d",&minute)==1)
{
minute1=hour'50';
total=minute+hour;
printf("enter the total minute=>%d",total);
else
{
printf("error the correct value");
'}
else
{
printf("error the correct value");
}
}
