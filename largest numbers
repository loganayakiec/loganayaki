#include<stdio.h>
int main()
{
int a,b,c;
print("entere the three numbers");
scanf("%d%d%d",&a&b&c);
if(a>=b&&a>=c)
{
printf("the number is larger");
}
if(b>=a&&b>=c)
{
printf("the number is larger");
}
else
{
printf("the number is larger");
}
return 0;
}
